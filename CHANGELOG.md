# 更新日志 (Changelog)

Agent Status Board —— macOS 桌面悬浮组件 + 菜单栏指示灯，实时显示 Codex / Claude Code 的会话状态与用量。

版本号即 `VERSION` 文件，App 会按它做在线自更新（详见 README / AI-SETUP.md）。

---

## v1.18
- **修复:已删除 / 已结束的会话还显示「正在执行」**。之前用 transcript 文件的修改时间判断会话是否在运行,但后台写入(标题自动生成、删除时碰到文件)会刷新这个时间,让已死 / 已删的会话看起来还在跑。改为只认 hook 自己的时间戳;hook 静默超过 15 分钟的「运行中」会话直接从看板移除(不再显示正在执行,也不会伪装成已完成)。
- **修复:点更新、下载完成后没有自动重启**。自更新脚本以 App 的 launchd 作业子进程身份运行,旧的重启方式(`launchctl unload`/`load`)会把作业重载进错误的域,导致 App 重装后不出现。改为显式定位用户 GUI 会话(`launchctl kickstart`/`bootstrap gui/<uid>`),并以 `open` 兜底,确保下载完成后稳定重启。

## v1.17
- **菜单栏角标只显示需要关心的**:右上角的任务数角标只统计「进行中 / 需处理」的任务,**已完成的不再计入**;某个工具全部完成时**不显示角标**,只留余量水球。小小的菜单栏不浪费在已经结束的事情上。

## v1.16
- **用量改为显示「余量(剩余)」**:菜单栏水球与桌面进度条都直接显示「还剩多少」(像电量 / 跟 Codex 一致)——CC 用了 17% 就显示「剩 83%」,不用自己算;满格绿 → 随消耗 → 黄 → 红(快耗尽)。
- **菜单栏水球美化**:竖向渐变绿(上浅下深)、放大到填满菜单栏高度、剩余数字加大加粗;进行中的任务数从「球旁孤立的数字」改成「贴在球右上角的状态色徽章」(绿/黄/红)。

## v1.15
- **菜单栏角标重做成「液态玻璃球」**：每个工具（Codex/CC）一个球；液面 = 该工具 5 小时用量（随用量往上涨，绿→黄→红）、球内数字 = 用量 %、球边数字 = 进行中任务数（按最高优先级状态着色）；亮色系、带水波晃动动画；放大到菜单栏高度。
- **菜单栏下拉重建**：点击角标弹出的下拉，改为与桌面悬浮组件一致的内容（需处理 / 正在执行 / 最近完成 / 用量），移除过时的「活跃会话 / 活动」布局。
- **清理"僵尸"会话**：被关闭/杀掉但没触发 Stop 的会话会一直卡在"正在执行"。现在超过 15 分钟没有真实活动（transcript/rollout 未更新）即判定为已结束，自动移出"正在执行"。
- **Codex 钩子不再每次更新都要重新信任**：根因是安装脚本每次都重写 config.toml 的 hooks 块，把 Codex 存在其中的 `[hooks.state]` 信任表一起删了。现在脚本对已就绪的钩子完全不改动（保留信任），必须改写时也会保留并外移该信任表；自更新会刷新钩子脚本但绝不碰 config.toml。
- **跨天保留最近完成**：底部"今日完成"改为"最近完成"，跨重启/跨天保留最近 5 个已完成会话（按会话去重、可点击回到会话继续、带"今天/昨天/日期"时间），明早一眼就能看到昨天干到哪、点一下接着干。

## v1.14
- **发布到 GitHub 并打通在线自更新闭环**：App 每小时查仓库 `VERSION`，有新版即在面板顶部弹「有新版本 · 点击更新」，点一下自动从 GitHub 下载源码、本地重编、重装、重启。
- 自更新刷新本地源码时不再误删 `.build` / `dist` 编译缓存。

## v1.13
- **自更新改为"下载源码包"而非 git**：收到 zip 的人无需 git，App 用 HTTP 拉取 GitHub 源码归档即可更新（只需 Swift 工具链）。更新源写在随包分发的 `update-source.json`。
- 本地编译，**无需代码签名 / 公证 / Apple 开发者账号**，也不被 Gatekeeper 拦截。

## v1.12
- 首版"源码自更新"（基于 git，已被 v1.13 的 zip 友好方案取代）。

## v1.11
- **重做 App 图标**：换成"霓虹脉冲波形 + 雷达环"的暗色磨砂玻璃风格，更具科技感（百炼文生图生成）。

## v1.10
- **未安装的工具不再显示其用量**：没有 Codex 就不显示 Codex、没用过 CC 就不显示 CC（不再有"CC 用量获取中"的空占位）。
- **菜单栏角标重做**：三颗扁平小圆点 → 一颗玻璃质感状态球 + 活跃计数，颜色随状态（红闪/琥珀呼吸/绿常亮）。

## v1.9
- 新增 App 图标；安装指南 `AI-SETUP.md` 更清晰（自动 vs 需用户操作分明）。
- 修正 Codex hooks 的信任引导：在「设置 → 编码 → 钩子 → 用户配置」里信任（Codex 没有 `/hooks` 命令）。

## v1.8
- **隐藏已删除 / 已归档的会话**：CC 里删掉的、Codex 里归档的会话不再出现在面板（按底层 transcript / rollout 是否还在判断；恢复后自动重新显示）。

## v1.7
- **Codex 用量改为实时**：通过 Codex 自己的后端用量端点获取（免费、非推理调用），不再是本地 rollout 的滞后快照。

## v1.6
- 诚实标注 Codex 用量的"快照时间"：旧快照会标「N 前快照」并把进度条变暗，避免误读成实时。

## v1.5
- **玻璃质感整体重构**：3D 玻璃球指示灯（高光 + 外发光）、更粗的圆角渐变用量条、用量图例（充足/偏高/耗尽）、深蓝氛围光卡片、行悬停滑动高亮、出现时淡入。

## v1.4
- **新增 Claude Code 用量面板**：5 小时 / 每周用量 + 重置倒计时（用极小推理 ping 读响应头获取，token 由 `cc-token-setup.sh` 生成）。

## v1.3
- 窗口改为"顶部锚定、向下伸缩"（增删会话时顶边不再乱跑）。
- 新增 Codex 用量面板。

## v1.2
- **点击任务行可打开对应 App / 会话**（Codex 直达 thread，CC 前台 Claude）。
- 「正在执行 / 今日完成」改为完整明细列表，不再折叠成计数。

## v1.1
- **"需要处理"分类器可移植**：支持任意 OpenAI 兼容厂商（百炼 / OpenAI / DeepSeek / Ollama…），含交互配置向导。
- 桌面组件可拖动定位（非激活面板，不抢焦点）；改用 Codex / CC 官方生命周期 hooks 实时驱动状态。

## v1.0
- 首个版本：菜单栏三灯指示 + 下拉面板，事件驱动（hooks 写状态、Swift 每 3s 读），显示 Codex / Claude Code 会话状态与活动时间线。
